class Board{
    field int size;
    field Array data;
    constructor Board new(int _size){
        let size = _size;
        let data = Array.new(size);
        var i;
        let i=0;
        while (~(i==size)){
            let data[i] = Array.new(size);
            let i=i+1;
        }
    }
}