class Renderer {
    field Board board;
    constructor Renderer new(Board _board){
        let board = _board;
        return this;
    }

    method void draw(){
        do DrawHelper.drawCircle(100,100,100,5,true);
        do DrawHelper.drawCircle(50,100,30,12,true);
        do DrawHelper.drawCircle(150,100,30,12,true);
        return;
    }
}